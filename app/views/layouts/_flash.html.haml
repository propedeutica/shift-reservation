- flash.each do |message_type, message|
  %div{:class => "#{flash_class(message_type) + " alert-dismissable"}"}
    %button.close{"area-hidden" => "true", "data-dismiss" => "alert", :type => "button"}
      %span.pficon.pficon-close
    %span{:class => "#{flash_icon(message_type)}"}
    = message
